import { ModelCatalogView } from "@/views/models/model-catalog-view";

export const metadata = {
	title: "Model Catalog | AXion Hub",
};

export default function ModelCatalogPage() {
	return <ModelCatalogView />;
}
