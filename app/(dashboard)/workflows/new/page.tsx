import { WorkflowEditorView } from "@/views/workflows/workflow-editor-view";

export const metadata = {
	title: "New Workflow | AXion Hub",
};

export default function NewWorkflowPage() {
	return <WorkflowEditorView />;
}
