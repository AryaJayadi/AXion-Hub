import { PluginInstallView } from "@/views/plugins/plugin-install-view";

export const metadata = {
	title: "Install Plugin | AXion Hub",
};

export default function PluginInstallPage() {
	return <PluginInstallView />;
}
